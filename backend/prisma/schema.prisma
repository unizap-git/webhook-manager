// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id          String   @id @default(cuid())
  name        String?
  email       String   @unique
  password    String
  accountType String   @default("PARENT") @map("account_type") // PARENT or CHILD
  parentId    String?  @map("parent_id")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  // Relations
  parent             User?               @relation("ParentChild", fields: [parentId], references: [id], onDelete: Cascade)
  childAccounts      User[]              @relation("ParentChild")
  projects           Project[]           @relation("ProjectOwner")
  projectAccess      ProjectAccess[]     @relation("UserAccess")
  grantedAccess      ProjectAccess[]     @relation("UserGrantedBy")
  userVendorChannels UserVendorChannel[]
  messages           Message[]
  analyticsCache     AnalyticsCache[]
  outboundMessages   OutboundMessage[]

  @@map("users")
}

model Vendor {
  id          String  @id @default(cuid())
  name        String  @unique // msg91, karix, aisensy, sendgrid
  slug        String  @unique // url-friendly version
  description String?
  isActive    Boolean @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at")

  // Relations
  userVendorChannels UserVendorChannel[]
  messages           Message[]
  analyticsCache     AnalyticsCache[]
  outboundMessages   OutboundMessage[]

  @@map("vendors")
}

model Channel {
  id          String  @id @default(cuid())
  name        String  @unique // SMS, WhatsApp, Email
  type        String  // sms, whatsapp, email
  description String?
  isActive    Boolean @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at")

  // Relations
  userVendorChannels UserVendorChannel[]
  messages           Message[]
  analyticsCache     AnalyticsCache[]
  outboundMessages   OutboundMessage[]

  @@map("channels")
}

model UserVendorChannel {
  id            String  @id @default(cuid())
  userId        String  @map("user_id")
  vendorId      String  @map("vendor_id")
  channelId     String  @map("channel_id")
  projectId     String  @map("project_id")
  webhookUrl    String  @unique @map("webhook_url")
  webhookSecret String? @map("webhook_secret")
  isActive      Boolean @default(true) @map("is_active")
  createdAt     DateTime @default(now()) @map("created_at")

  // Relations
  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  vendor  Vendor  @relation(fields: [vendorId], references: [id])
  channel Channel @relation(fields: [channelId], references: [id])
  project Project @relation(fields: [projectId], references: [id], onDelete: Cascade)

  @@unique([userId, vendorId, channelId, projectId])
  @@map("user_vendor_channels")
}

model Message {
  id             String    @id @default(cuid())
  userId         String    @map("user_id")
  vendorId       String    @map("vendor_id")
  channelId      String    @map("channel_id")
  projectId      String    @map("project_id")
  recipient      String
  messageId      String?   @map("message_id") // vendor-provided message ID
  contentSummary String?   @map("content_summary")
  createdAt      DateTime  @default(now()) @map("created_at")

  // Relations
  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  vendor  Vendor  @relation(fields: [vendorId], references: [id])
  channel Channel @relation(fields: [channelId], references: [id])
  project Project @relation(fields: [projectId], references: [id], onDelete: Cascade)
  events  MessageEvent[]

  // Indexes for analytics queries
  @@index([userId, createdAt])
  @@index([userId, projectId, createdAt])
  @@index([vendorId])
  @@index([channelId])
  @@map("messages")
}

model MessageEvent {
  id           String   @id @default(cuid())
  messageId    String   @map("message_id")
  vendorRefId  String?  @map("vendor_ref_id") // Extracted vendor reference ID (requestId, sg_message_id, etc.)
  status       String   // sent, delivered, read, failed, bounced
  reason       String?  // failure reason if applicable
  timestamp    DateTime @default(now())
  rawPayload   String?  @map("raw_payload") // JSON string of webhook payload

  // Denormalized fields for query optimization (eliminates JOINs)
  userId       String?  @map("user_id")
  vendorId     String?  @map("vendor_id")
  channelId    String?  @map("channel_id")
  projectId    String?  @map("project_id")

  // Relations
  message Message @relation(fields: [messageId], references: [id], onDelete: Cascade)

  // Indexes for analytics queries
  @@index([messageId, timestamp])
  @@index([messageId]) // Standalone index for JOINs
  @@index([status])
  @@index([vendorRefId]) // For matching with outbound messages
  @@index([userId, status, timestamp]) // Optimized for analytics groupBy queries
  @@index([userId, vendorId, channelId, projectId, status]) // Full analytics filter
  @@map("message_events")
}

model AnalyticsCache {
  id             String   @id @default(cuid())
  userId         String   @map("user_id")
  vendorId       String?  @map("vendor_id")
  channelId      String?  @map("channel_id")
  projectId      String?  @map("project_id")
  date           DateTime
  totalSent      Int      @default(0) @map("total_sent")
  totalDelivered Int      @default(0) @map("total_delivered")
  totalRead      Int      @default(0) @map("total_read")
  totalFailed    Int      @default(0) @map("total_failed")
  successRate    Float    @default(0) @map("success_rate")
  lastUpdated    DateTime @updatedAt @map("last_updated")

  // Relations
  user    User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  vendor  Vendor?  @relation(fields: [vendorId], references: [id])
  channel Channel? @relation(fields: [channelId], references: [id])
  project Project? @relation(fields: [projectId], references: [id], onDelete: Cascade)

  @@unique([userId, vendorId, channelId, projectId, date])
  @@map("analytics_cache")
}

model Project {
  id          String   @id @default(cuid())
  name        String
  description String?
  userId      String   @map("user_id") // Owner
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  // Relations
  owner              User                @relation("ProjectOwner", fields: [userId], references: [id], onDelete: Cascade)
  projectAccess      ProjectAccess[]
  userVendorChannels UserVendorChannel[]
  messages           Message[]
  analyticsCache     AnalyticsCache[]
  outboundMessages   OutboundMessage[]

  @@unique([name, userId]) // Project names must be unique per user
  @@map("projects")
}

model ProjectAccess {
  id         String   @id @default(cuid())
  projectId  String   @map("project_id")
  userId     String   @map("user_id") // Child account
  accessType String   @map("access_type") // 'specific' or 'all'
  grantedBy  String   @map("granted_by") // Parent account who granted access
  createdAt  DateTime @default(now()) @map("created_at")

  // Relations
  project   Project @relation(fields: [projectId], references: [id], onDelete: Cascade)
  user      User    @relation("UserAccess", fields: [userId], references: [id], onDelete: Cascade)
  grantedByUser User @relation("UserGrantedBy", fields: [grantedBy], references: [id])

  @@unique([projectId, userId]) // One access record per project-user pair
  @@map("project_access")
}

// Outbound Message Logging - for tracking messages sent by users
model OutboundMessage {
  id          String   @id @default(cuid())
  userId      String   @map("user_id")
  projectId   String   @map("project_id")
  vendorId    String   @map("vendor_id")
  channelId   String   @map("channel_id")
  vendorRefId String   @map("vendor_ref_id") // The requestId/sg_message_id from vendor response
  recipient   String   // Phone number or email
  content     String   // Original message content
  sentAt      DateTime @map("sent_at") // When the message was actually sent
  createdAt   DateTime @default(now()) @map("created_at") // When logged to our system

  // Relations
  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  project Project @relation(fields: [projectId], references: [id], onDelete: Cascade)
  vendor  Vendor  @relation(fields: [vendorId], references: [id])
  channel Channel @relation(fields: [channelId], references: [id])

  // Indexes for efficient matching and queries
  @@index([vendorRefId]) // Primary index for matching with webhook events
  @@index([userId, projectId, sentAt])
  @@index([userId, vendorId, channelId])
  @@map("outbound_messages")
}